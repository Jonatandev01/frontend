# Alojamientos Frontend (Angular 17+)

Minimal scaffold generated to connect to a Laravel backend at http://localhost:8000.

## Instalación

```bash
npm install
ng serve
# Open http://localhost:4200
```

## Notas
- Tailwind config included (install tailwind and setup per usual).
- Uses standalone components and Http interceptor for token.
- Ensure backend CORS allows localhost:4200.